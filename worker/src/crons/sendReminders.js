export async function sendReminders(db, resendApiKey) {
  // Phase 7: wire up Resend email reminders
  console.log('sendReminders cron fired — email wiring coming in Phase 7');
  return { sent: 0 };
}
