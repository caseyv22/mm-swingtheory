export async function sendReminders(db, resendApiKey) {
  // Phase 5: wire up Resend email reminders
  // Find all sessions tomorrow, find confirmed bookings, send reminder emails
  console.log('sendReminders cron fired — email wiring coming in Phase 5');
  return { sent: 0 };
}
